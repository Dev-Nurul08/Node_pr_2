<!doctype html>
<html lang="en">

<head>
    <title>Edit Task</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #111119;
            color: white;
            min-height: 100vh;
            display: flex;
            align-items: center;
        }

        .edit-card {
            background: #1e1e2f;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
            width: 100%;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="edit-card">
                    <h2 class="text-center mb-4 text-warning">Edit Task</h2>
                    <form method="POST" action="/edit/task/<%= task.id %>">
                        <div class="mb-3">
                            <label class="form-label text-secondary small text-uppercase">Task Description</label>
                            <input type="text" name="text" value="<%= task.text %>"
                                class="form-control bg-dark text-white border-secondary form-control-lg" required>
                        </div>
                        <div class="mb-4">
                            <label class="form-label text-secondary small text-uppercase">Due Date</label>
                            <input type="date" name="dueDateTime"
                                value="<%= new Date(task.dueDate).toISOString().split('T')[0] %>"
                                class="form-control bg-dark text-white border-secondary">
                        </div>
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-warning btn-lg fw-bold">Save Changes</button>
                            <a href="/" class="btn btn-link text-secondary">Cancel and Go Back</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>

</html>